# Legacy Migrations

These migrations have already been applied to the database. They are kept here for reference only.

**Do not add new migrations here.** New migrations go in `supabase/migrations/` with the format `YYYYMMDD_beskrivelse.sql`.
